diff --git a/src/main/resources/static/css/style.css b/src/main/resources/static/css/style.css
index 366d1ef..fd3193a 100644
--- a/src/main/resources/static/css/style.css
+++ b/src/main/resources/static/css/style.css
@@ -71,25 +71,25 @@ h1 {
 }
 
 /* Reset Button */
+.refresh-btn,
 .reset-btn {
     display: inline-flex;
     align-items: center;
     gap: 6px;
-    background: transparent;
-    color: #888;
-    border: 1.5px solid #ddd;
-    padding: 9px 16px;
-    border-radius: 30px;
-    font-weight: 600;
+    padding: 8px 16px;
+    border-radius: 20px;
+    border: 1px solid #e0e0e0;
+    background: white;
+    font-size: 14px;
     cursor: pointer;
     transition: all 0.2s;
-    font-size: 0.9rem;
+    color: #666;
 }
 
+.refresh-btn:hover,
 .reset-btn:hover {
     background: #f5f5f5;
-    color: #555;
-    border-color: #bbb;
+    border-color: #ccc;
 }
 
 .upload-status {
diff --git a/src/main/resources/static/index.html b/src/main/resources/static/index.html
index 464bae5..c586277 100644
--- a/src/main/resources/static/index.html
+++ b/src/main/resources/static/index.html
@@ -14,6 +14,16 @@
         <div class="header-content">
             <h1>绮惧噯鑱屼綅鍖归厤</h1>
             <div class="header-actions">
+                <div id="authContainer" class="auth-container" style="display: flex; align-items: center; gap: 10px;">
+                    <a href="/oauth2/authorization/google" id="loginBtn" class="refresh-btn"
+                        style="text-decoration: none;">Login with Google</a>
+                    <div id="userInfo" style="display: none; align-items: center; gap: 10px;">
+                        <span id="userName" style="font-weight: bold; color: #333;"></span>
+                        <form action="/logout" method="post" style="margin: 0;">
+                            <button type="submit" class="reset-btn" style="display: inline-flex;">Logout</button>
+                        </form>
+                    </div>
+                </div>
                 <div class="upload-container">
                     <input type="file" id="resumeFile" accept="application/pdf" style="display: none;">
                     <label for="resumeFile" class="upload-btn">
@@ -24,6 +34,13 @@
                         涓婁紶 PDF 绠€鍘嗚繘琛屽尮閰?                     </label>
                 </div>
+                <button id="refreshBtn" class="refresh-btn" onclick="refreshJobs()">
+                    <svg viewBox="0 0 24 24" width="16" height="16" fill="currentColor">
+                        <path
+                            d="M17.65 6.35C16.2 4.9 14.21 4 12 4c-4.42 0-7.99 3.58-7.99 8s3.57 8 7.99 8c3.73 0 6.84-2.55 7.73-6h-2.08c-.82 2.33-3.04 4-5.65 4-3.31 0-6-2.69-6-6s2.69-6 6-6c1.66 0 3.14.69 4.22 1.78L13 11h7V4l-2.35 2.35z" />
+                    </svg>
+                    鍒锋柊
+                </button>
                 <button id="resetBtn" class="reset-btn" style="display: none;" onclick="resetState()">
                     <svg viewBox="0 0 24 24" width="16" height="16" fill="currentColor">
                         <path
@@ -42,7 +59,7 @@
         </div>
         <div id="loadingIndicator" class="loading" style="display: none;">
             <div class="spinner"></div>
-            <p>姝ｅ湪鍒嗘瀽骞跺尮閰嶄笓灞炶亴浣?..</p>
+            <p id="loadingText">姝ｅ湪鍔犺浇鑱屼綅...</p>
         </div>
     </main>
 
@@ -84,6 +101,12 @@
         let analysisCache = {}; // Cache: jobId -> analysis text
         let currentJobData = null;
 
+        // Pagination state
+        let currentPage = 0;
+        const pageSize = 10;
+        let isLoading = false;
+        let hasMore = true;
+
         document.getElementById('resumeFile').addEventListener('change', async (e) => {
             const file = e.target.files[0];
             if (!file) return;
@@ -110,13 +133,11 @@
                     // Show reset button
                     document.getElementById('resetBtn').style.display = 'inline-flex';
 
-                    document.getElementById('jobGrid').innerHTML = '';
-                    document.getElementById('loadingIndicator').style.display = 'flex';
+                    // Reset and fetch
+                    refreshJobs();
 
-                    // Poll for match results since backend processing is async via MQ
-                    setTimeout(fetchJobs, 2000);
-                    setTimeout(fetchJobs, 5000);
-                    setTimeout(fetchJobs, 10000);
+                    // Poll for initial match results (backend processing might be async)
+                    setTimeout(refreshJobs, 2000);
                 } else {
                     statusDiv.textContent = '涓婁紶澶辫触锛岃閲嶈瘯';
                     statusDiv.className = 'upload-status error';
@@ -139,35 +160,52 @@
 
             document.getElementById('resetBtn').style.display = 'none';
             document.getElementById('resumeFile').value = '';
-            document.getElementById('jobGrid').innerHTML = '';
-            document.getElementById('loadingIndicator').style.display = 'none';
 
-            // Reload default jobs
-            fetchJobs();
+            refreshJobs();
+        }
+
+        function refreshJobs() {
+            currentPage = 0;
+            hasMore = true;
+            document.getElementById('jobGrid').innerHTML = '';
+            fetchJobs(true);
         }
 
-        async function fetchJobs() {
-            let url = '/api/jobs/match';
+        async function fetchJobs(isInitial = false) {
+            if (isLoading || !hasMore) return;
+
+            isLoading = true;
+            const loader = document.getElementById('loadingIndicator');
+            loader.style.display = 'flex';
+
+            let url = `/api/jobs/match?page=${currentPage}&size=${pageSize}`;
             if (currentResumeId) {
-                url += `?resumeId=${currentResumeId}`;
+                url += `&resumeId=${currentResumeId}`;
             }
 
             try {
                 const response = await fetch(url);
                 const jobs = await response.json();
-                renderJobs(jobs);
+
+                if (jobs.length < pageSize) {
+                    hasMore = false;
+                }
+
+                renderJobs(jobs, isInitial);
+                currentPage++;
             } catch (error) {
                 console.error('Failed to fetch jobs', error);
             } finally {
-                document.getElementById('loadingIndicator').style.display = 'none';
+                isLoading = false;
+                loader.style.display = 'none';
             }
         }
 
-        function renderJobs(jobs) {
+        function renderJobs(jobs, clearGrid = false) {
             const grid = document.getElementById('jobGrid');
-            grid.innerHTML = '';
+            if (clearGrid) grid.innerHTML = '';
 
-            if (jobs.length === 0) {
+            if (jobs.length === 0 && grid.children.length === 0) {
                 grid.innerHTML = '<div style="grid-column: 1/-1; text-align: center; padding: 40px; color: #888;">鏆傛椂娌℃湁鎵惧埌鍖归厤鐨勮亴浣?/div>';
                 return;
             }
@@ -289,8 +327,27 @@
             if (e.key === 'Escape') closeModal();
         });
 
-        // Fetch generic jobs on load
-        fetchJobs();
+        // Infinite Scroll Listener
+        window.addEventListener('scroll', () => {
+            if (window.innerHeight + window.scrollY >= document.body.offsetHeight - 500) {
+                fetchJobs();
+            }
+        });
+
+        // Initial load
+        refreshJobs();
+
+        // Check Authentication Status
+        fetch('/user/me')
+            .then(res => res.ok ? res.json() : null)
+            .then(data => {
+                if (data && data.name) {
+                    document.getElementById('loginBtn').style.display = 'none';
+                    document.getElementById('userInfo').style.display = 'flex';
+                    document.getElementById('userName').textContent = 'Hi, ' + data.name;
+                }
+            })
+            .catch(err => console.log('Not authenticated:', err));
     </script>
 </body>
 
